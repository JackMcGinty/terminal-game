[package]
name = "terminal-pong"
version = "0.1.0"
edition = "2021"
workspace = "../"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# local crate access
terminal-draw = { path = "../terminal-draw" }